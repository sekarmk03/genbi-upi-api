const err = require('../common/custom_error');
const { commentSvc } = require('../services');

module.exports = {
    index: async (req, res, next) => {
        try {
            
        } catch (error) {
            next(error);
        }
    }
}