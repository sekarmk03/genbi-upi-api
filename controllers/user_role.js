const { userRoleSvc } = require('../services');
const err = require('../common/custom_error');

module.exports = {
    index: async (req, res, next) => {
        try {
            
        } catch (error) {
            next(error);
        }
    }
}